
  TODO list file

- Add unit tests for all base classes
- Add base collection. It should solve model dependency issue and, maybe parameters issue (see OmniRetail base collection)
- Fix cache property
- findView() query argument is very long. I should think about decrease of it. For example, it should try to find specified
  view by alias in nested views too.
- Write about global configuration